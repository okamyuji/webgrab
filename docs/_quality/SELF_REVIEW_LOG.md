# セルフレビュー記録

## 2026-08-27 docs/08-js-render-design.md v1.6 seal（実装前probe補遺）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanでerror 0 |
| 2 | 相互参照 | §1 probe(13)〜(15)を§4.2手順0・6と§5 render.rs行、09計画書Task 8の確定事項表から参照 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | 「分離ワールドは`goto`前に作る」（v1.5手順0）を「`goto`後に作る」へ訂正し、手順0の記述と手順3・6を同じ内容に揃えた |
| 5 | 状態機械 | 変更なし |
| 6 | 不変条件 | DOM取得を分離ワールドに変えても、終了コード表・`--max-bytes`の計上・終了コード8の優先判定は不変 |
| 7 | 契約の伝播 | `page.content()`不使用を§3決定表（DOM安定の計測）・§4.2手順6・§5・09 Task 8コードに伝播 |
| 8 | 置換安全 | 文単位の完全一致置換（`assert`）。副作用なし |
| 9 | スコープ境界 | 変更なし |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC v1.2（R9）は08 v1.6にも対応 |

## 2026-08-27 docs/08-js-render-design.md v1.5 seal（レビューRound 4反映後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanでerror 0 |
| 2 | 相互参照 | §4.2手順0/6/7が§3決定表（`--max-bytes`計上、DOM安定、sandbox）・§5 render.rs行・§6 E15・§9-9と対応。§4.5の04更新項目が04:58/60/122/179/204を名指ししていることを確認。probe(10)〜(12)を§1に記録 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | §2/§10の対象外から「interceptの並列化」を外し「子セッションへのFetch/Network付与」に置換（§4.2の個別タスク化と整合）。終了コード8の評価順を§3・§4.2手順7・§4.3手順5で同一内容に統一 |
| 5 | 状態機械 | `render_status`5状態は不変。`fallback_reason`に`Phase`を導入し、終了コード4の2事象を分離 |
| 6 | 不変条件 | 終了コード表は不変。`--max-bytes`は両経路とも展開後バイト（render経路は累計+DOM長）。`--raw`の免除を維持 |
| 7 | 契約の伝播 | `--wait-ms`の`Option`化を§4.4フラグ表・§5 cli.rs・`flag-ignored`判定・継続コマンド再現に伝播。`Fetched`の消費バイトを§5 fetch.rs行と§4.3手順1に伝播。YAMLのSHAピン留めを§7本文と§9-9に伝播 |
| 8 | 置換安全 | 文単位の完全一致置換（`assert`）と`runs-on`の全置換（3箇所とも意図どおり）。副作用なし |
| 9 | スコープ境界 | 既定Chrome起動・中央workflow・子セッション付与は対象外のまま。interceptの個別タスク化は所見の根本原因への対処として対象に入れた（§2/§10を同時更新） |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC v1.2（R9）は08 v1.5にも対応。E2E件数（15）はルーブリックの列挙対象外 |

## 2026-08-27 docs/08-js-render-design.md v1.3（レビューRound 3反映後、独立再レビューなし）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanでerror 0 |
| 2 | 相互参照 | §4.2手順4の`is_main_navigation`を§5 wait.rs・§6単体に、§4.3の`fallback_reason`を§5 pipeline・§6単体・§9-6に、`visible_text_len`を§3・§4.3・§5 convert.rs・§6に伝播したことを確認。§9-4のE番号列が§6の表と一致 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | 終了コード8の優先判定（§3・§4.2手順6・§4.3手順5）が3箇所で同じ内容。エスカレーション判定（可視テキスト）と既存判定（変換後本文）の非対称を§3と§4.3で同じ説明にした |
| 5 | 状態機械 | `render_status`5状態は変更なし。`no-gain`に`reason=shorter`を付与し、行を出す3状態すべてにreasonが定義された |
| 6 | 不変条件 | 終了コード表は不変。終了コード8は出力なし中断を維持。`--raw`の免除を維持 |
| 7 | 契約の伝播 | §4.5の04更新項目にshort-content提案規則・§7の提案・継続コマンド規則(1)例外・終了コード6書式差を追加 |
| 8 | 置換安全 | 文単位の完全一致置換（`assert old in s`）。副作用なし |
| 9 | スコープ境界 | 並列化・中央workflow・既定Chrome起動は対象外のまま。`visible_text_len`の追加は判定に必要な最小関数 |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC v1.2（R9）は08 v1.3にも対応 |

## 2026-08-27 docs/08-js-render-design.md v1.2（レビューRound 2反映後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanでerror 0（文末コロン1件を文に書き換え） |
| 2 | 相互参照 | §4.1の提案規則を§4.3 4・5と§6 E8が参照、§4.4の環境変数表を§6が参照、§7のコマンドを§9 5が参照することを確認。chromiumoxide `BrowserConfig::builder().no_sandbox()`と`EventRequestPaused.network_id`をソースで確認 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | 「捨てない」原則と終了コード8伝播の関係を決定表の理由欄で明示。§2の対象外と§4.3 5のフォールバック範囲（終了コード7・max-bytes・extract）が矛盾しないことを確認 |
| 5 | 状態機械 | `render_status`5状態の遷移を§4.3 1〜5に割り当て（static: 判定不発／エラー伝播、skipped: 2、rendered/no-gain: 4、failed: 5）。到達不能なし |
| 6 | 不変条件 | 終了コード表は不変。`--raw`の終了コード6/short-content免除を維持。短文閾値200を可視テキスト基準で共有 |
| 7 | 契約の伝播 | `static_chars`/`rendered_chars`をoutput.rs・§4.4・E10bに、`--no-sandbox`をcli.rs・§4.4・継続コマンド規則・README・E2E環境変数表に、`hint_for`を§4.1・§4.3・§5・§6単体テストに伝播 |
| 8 | 置換安全 | 文単位の完全一致置換（Pythonで`assert old in s`）。再走査で副作用なし |
| 9 | スコープ境界 | 並列化・中央workflow・既定Chrome起動を§2/§10で対象外のまま維持 |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC v1.2（R9）は08 v1.2にも対応。E2E件数（12）はルーブリックの列挙対象外 |

## 2026-08-27 docs/08-js-render-design.md v1.1（レビューRound 1反映後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanでerror 0（常体warnのみ。04-design.mdに合わせ常体を維持） |
| 2 | 相互参照 | 04 §3/§3.1/§4/§5/§6/§7/§8/§9、chromiumoxide `src/detection.rs`・`src/handler/target.rs`、dom_smoothie `src/lib.rs`の`ReadabilityError`を参照先として確認 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | §2の「暗黙フォールバック対象外」を`--render`明示時に限定し、`--auto-render`時の例外を04 §3へ追記する旨を§4.5に明記。04 §6の継続コマンド規則(1)に規則(4)を追加する旨を明記 |
| 5 | 状態機械 | `render_status`の5状態（static/rendered/failed/no-gain/skipped）を§4.3の各分岐に割り当て、到達不能な状態が無いことを確認 |
| 6 | 不変条件 | 終了コード表は不変。終了コード8/4はフォールバックで飲み込まない。短文閾値200を待機・判定・short-contentで共有 |
| 7 | 契約の伝播 | `render_status`をoutput.rs・04 §6（json/frontmatter）・E6/E10/E11に伝播。`hint=`トークンを§4.1・§4.3・統合テスト・E8に伝播。`extra_flags`の置換規則を§4.3 6・単体テスト・E6に伝播 |
| 8 | 置換安全 | 全面書き直しのため機械置換なし |
| 9 | スコープ境界 | 既定Chrome起動・中央workflow変更・intercept並列化を§2と§10の両方で対象外と宣言 |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC v1.2（R9）は08 v1.1にも対応。E2E件数の増加（8→11）はルーブリックの列挙対象外 |

## 2026-08-27 docs/08-js-render-design.md v1.0（初版執筆後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | ja-style scanで検出5件を文ごと書き直し、再走査で0件 |
| 2 | 相互参照 | 04-design.md §3/§3.1/§5/§7/§9の参照先が実在することを確認。chromiumoxide `src/page.rs` `goto`と`src/handler/network.rs`の`Network.enable`をソースで確認 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | 04 §3「暗黙フォールバック不採用」とはopt-inフラグ+stderr明示で両立。04 §5の`--wait-ms`既定2000との差は§4.5でv1.3更新対象として明記 |
| 5 | 状態機械 | 該当なし（待機ループは§4.2で終了条件を列挙） |
| 6 | 不変条件 | 終了コード表を変更しない。短文閾値200を待機条件・エスカレーション判定・short-contentで共有 |
| 7 | 契約の伝播 | JSON `rendered`追加をoutput.rs・04 §6・E6テストの3箇所に記載。`extra_flags`の置換規則を§4.3と§6単体テストに記載 |
| 8 | 置換安全 | 文単位の完全一致置換のみ（3文）。再走査で副作用なし |
| 9 | スコープ境界 | 既定でのChrome起動・暗黙フォールバック・中央workflow変更を§2と§10の両方で対象外と宣言 |
| 10 | ルーブリック鮮度 | QUALITY_RUBRICをv1.2へ更新し08を対象に追加、R9を新設 |

## 2026-07-17 docs/04-design.md v1.2（レビューRound 2反映後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | 目視で問題なし |
| 2 | 相互参照 | §3.1への参照（決定表・§4 netguard/robots説明）が実在。終了コード表のerror=列と§7エラーパスの整合を確認 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | §3.1の手動リダイレクトとreqwest自動リダイレクト無効化が§7の「最大10ホップ」と整合。smoke test結果（tokio-runtime feature不要）を§3決定表・§8の両方に反映し矛盾解消 |
| 5 | 状態機械 | 該当なし |
| 6 | 不変条件 | --no-tokensでもchars/total保持（§6）により終端判定がトークン計測に依存しない不変条件を明記 |
| 7 | 契約の伝播 | 継続コマンド規則（--start-index置換・-o除外）を§6の全形式（markdown/frontmatter/json/text/html）に伝播 |
| 8 | 置換安全 | 機械置換なし（Editによる局所編集のみ）。終了コード表の重複ヘッダ混入をRead確認で検出し修正 |
| 9 | スコープ境界 | fetch専用を維持。SSRF対策・render interceptionはfetchの安全化でありスコープ拡大ではない |
| 10 | ルーブリック鮮度 | QUALITY_RUBRICをv1.1へ更新しR8（SSRF/ページング実装可能性）を追加、設計v1.2対応と明記 |


## 2026-07-17 docs/04-design.md v1.1（レビューRound 1反映後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | 目視で問題なし |
| 2 | 相互参照 | 03のD節注記と04の5章注記が相互に整合。§5フラグ表・§7エラーパス表・終了コード表（0〜8）を突合し一致 |
| 3 | 用語正準形 | doclint緑（0/0/0/0） |
| 4 | 決定矛盾 | 03の暗黙フォールバック行と終了コード行に置換注記を同一改訂で追加し解消 |
| 5 | 状態機械 | 該当なし |
| 6 | 不変条件 | 「文字=Unicodeスカラー値・半開区間」を全文字数表記に適用（§3定義、§5/§6が参照） |
| 7 | 契約の伝播 | 終了コード8追加を§5表・§7表・netguard説明の3箇所に反映。--no-tokens/--allow-privateをフラグ表とアーキテクチャ両方に反映 |
| 8 | 置換安全 | 全面書き直しのため機械置換なし |
| 9 | スコープ境界 | fetch専用を維持。--max-tokens等のスコープ拡大案は不採用としてバックログへ |
| 10 | ルーブリック鮮度 | ルーブリックv1.0の観点R1〜R7は§構成変更後も対応可能（§11追加は特別カテゴリで担保）。版数対応は再レビュー時にv1.1を明示 |


## 2026-07-17 docs/04-design.md v1.0（初版執筆後）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 境界スペース | 該当ルールなし（doclint対象外）。目視で問題なし |
| 2 | 相互参照 | 01/03への参照先の存在を確認。§5の終了コード表と§7のエラーパス表の対応を突合し一致 |
| 3 | 用語正準形 | doclint緑（Critical 0 / High 0 / Medium 0 / Low 0） |
| 4 | 決定矛盾 | 検出1件: 03-research-report.mdのB枝は「Chrome未検出時に静的取得へ暗黙フォールバック」、04-design.mdは「終了コード7で失敗」。設計側の決定表に不採用理由を明記して解消（同一編集内） |
| 5 | 状態機械 | 該当なし |
| 6 | 不変条件 | 該当なし |
| 7 | 契約の伝播 | 終了コード0〜7が§5表・§7表・§8テスト戦略で整合していることを確認 |
| 8 | 置換安全 | 機械置換は未実施 |
| 9 | スコープ境界 | fetch専用（検索・クローリング除外）を§2と§10の両方で宣言、本文中に逸脱なし |
| 10 | ルーブリック鮮度 | QUALITY_RUBRIC.md v1.0は設計書v1.0を対象と明記。観点R1〜R7と設計章立ての対応を確認 |
